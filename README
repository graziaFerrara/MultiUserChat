Per eseguire il progetto MultiUserChat in Eclipse ho dovuto:
• installare dal Marketplace di Eclipse il pacchetto e(fx)clipse (Help > Eclipse Marketplace)
• scaricare la SDK (io ho usato la versione 21) https://gluonhq.com/products/javafx/
• in Preferences > JavaFX mettere il path alla cartella lib della SDK
• in Preferences > User Libraries creare una nuova libreria (è sufficiente che contenga i jar contenuti nella cartella lib nella SDK)
• facendo tasto destro sul progetto in Build Path > Configure BuildPath > Libraries > Modulepath aggiungere la libreria creata 
• durante l'esecuzione in Run Configurations > Arguments > VM arguments inserire
    --module-path="/Library/Frameworks/javafx-sdk-21/lib" --add-modules=javafx.controls,javafx.fxml
    in quanto richiedeva il path alla SDK
• per evitare problemi al lancio, togliere la spunta all'opzione sottostante "Use the -XstartOnFirstThread argument when aunching with SWT"
    (sia per il run del server, sia per quello del client)
